apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    orgId: 1
    url: http://loki:3100
    basicAuth: false
    isDefault: false
    editable: true
    version: 1
    uid: "loki"
    jsonData:
      maxLines: 1000
      derivedFields:
        - datasourceUid: "tempo"
          matcherType: "label"
          matcherRegex: "trace_id"
          name: "TraceID"
          url: "$${__value.raw}"
          urlDisplayLabel: "View Trace"
          internal: true
